      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check JS Syntax
        run: |
          find . -name "*.js" -print0 | xargs -0 -n1 node --check