name: PHP Pipeline

on:
  push:
    branches:
      - master

jobs:
  php-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.0'

      - name: Syntax Check
        run: php -l index.php